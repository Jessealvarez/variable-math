//Create four variables, `x1`, `y1`, `x2`, and `y2` and store numbers in each of them.
//Calculate the distance between coordinates (x1, y1) and (x2, y2). 
//Log this distance out to the user in a human readable sentence.
//The distance formula along with an example can be found below.
let x1, x2, y1, y2, distance;
x1 = 3;
x2 = 2;
y1 = 4;
y2 = 1;
distance = 3;
console.log("Using the formula provided, the distance between the coordinates I chose is: " + distance );